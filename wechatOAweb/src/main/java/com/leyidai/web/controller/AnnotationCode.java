package com.leyidai.web.controller;

public class AnnotationCode {

//	boolean flagOrgid = false;
//	// 判断如果区县是使用自动审核的区县
//	if (dictionaryService.queryIsUseSocket(orderRecord.getTransactOrgId())) {
//		// 仅供演示用，开发区转换为河西的区县ID
//		if (orderRecord.getTransactOrgId() == 1105) {
//			flagOrgid = true;
//			orderRecord.setTransactOrgId(1041);
//		}
//		// 向接口发送数据审核
//		orderRecord.setId(0);
//		// JSONObject jsonObject = JSONObject
//		// .fromObject(webSockte(orderRecord));
//		// jsonObject.get("id")
//		// 如果返回值为1，认为验证通过,直接给到status=2
//		// if (jsonObject.get("returnMsg").toString().equals("1")) {
//		// orderRecord.setStatus(2);
//		// }
//
//		int type = switchType(orderRecord.getTransactTypeName());
//	}
//	// 如果orgid进行了测试socket时转换，则重新换回来，保证数据一致性
//	if (flagOrgid) {
//		orderRecord.setTransactOrgId(1105);
//	}
	
	// try {
	// jsonString=URLEncoder.encode(jsonString, "UTF-8");
	// } catch (UnsupportedEncodingException e1) {
	// // TODO Auto-generated catch block
	// e1.printStackTrace();
	// }
}
